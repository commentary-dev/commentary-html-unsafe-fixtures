# Baseline

The fixture branch adds unsafe and malformed HTML pages.

Use this baseline only as the stable `main` branch for the long-lived fixture PR. The review scenarios live on `fixture/unsafe-html`.
